[project]
name = "pypi-ci-testing"
version = "0.8.0.dev0"
description = "Add your description here"
requires-python = ">=3.12.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ty==0.0.80",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.15.0",
]
